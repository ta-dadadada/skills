name: implementation-loop
description: 'Convergence loop for implementing an already agreed change one acceptance
  criterion at a time: re-observe the repository, select one pending criterion, route
  its implementation through the relevant specialist workflow, prefer deterministic
  verification, review the result, continue independent work around blocked criteria,
  reopen criteria on findings, and finish with global verification plus an independent
  fresh-context full-diff review. Use after issue-kickoff or equivalent intake when
  settled criteria, dependencies, blockers, or review feedback require coordination
  across implementation cycles. Not for deriving scope or criteria, supplying domain-specific
  implementation methods, PR/commit planning, or a small direct change whose normal
  implementation workflow already provides a sufficient convergence path.'
license: MIT
type: skill
version: '0.1.0'
